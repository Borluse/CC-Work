首先加载 ../../Library/common/doc-rule.md 以了解必要规则
- 文档结构：需求简介 → 功能说明 → 配置项 → 接口设计 → 实现方案 → 注意事项
- **每个接口必须有注释**，说明用途和参数含义

# 需求文档模板

```markdown
# <需求标题>

## 需求简介

<一段话描述核心需求>

---

## 功能说明

<详细功能描述，分点列出，融合策划目标和技术行为>

### 边界条件

<特殊情况、异常处理，用表格列出>

---

## 配置项

<DataAsset 中的可配置参数，注明文件位置、类型和默认值>

---

## 接口设计

<核心接口的 C++ 声明，每个函数/属性上方添加注释说明用途和参数含义。蓝图接口用 > [!tip] 说明调用方式>

---

## 实现方案

### 涉及文件

<用表格列出相关文件/类/模块>

### 类图

<mermaid classDiagram：新增/修改的类、结构体及其关系，包含关键属性和方法>

### 关键流程

按功能拆分为多个子章节，每个子章节结构如下：

#### <流程名称>

<1-2 句话描述触发条件和核心逻辑>

##### 伪代码

每个接口/函数单独一个代码块，代码块前用一句话说明职责和触发时机。

##### 流程图

<mermaid flowchart：判断与分支>

##### 时序图

<mermaid sequenceDiagram：对象间调用顺序和消息传递>

---

## 注意事项

<性能、兼容性、边界情况、踩坑点。风险项用 > [!warning] 标注>

### 调试接口（CVar）（如需要）

| CVar | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `Cfr.Debug.<模块>.<功能>` | bool | false | 开启调试可视化 |
| `Cfr.Debug.<模块>.Disable` | bool | false | 强制关闭该功能 |

### 蓝图使用说明（如涉及蓝图）

- 在哪个蓝图类中调用
- 调用时机
- 输入参数来源
- 输出结果如何使用

---

## 待确认事项

<还未澄清的问题，留作后续跟进>

---

## 变更记录

- **<YYYY-MM-DD HH:mm>**：初始创建