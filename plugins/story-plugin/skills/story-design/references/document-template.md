首先加载 ../../Library/common/doc-rule.md 以了解必要规则
- **每个接口必须有注释**，说明用途和参数含义

# 需求文档模板

```markdown
# <需求标题>

## 1. 需求简介

<一段话描述核心需求>

## 2. 功能说明

<详细功能描述，分点列出，融合策划目标和技术行为>

### 2.1 边界条件

<特殊情况、异常处理，用表格列出>

## 3. 配置项

<DataAsset 中的可配置参数，注明文件位置、类型和默认值>

## 4. 接口设计

<核心接口的 C++ 声明，每个函数/属性上方添加注释说明用途和参数含义。蓝图接口用 > [!tip] 说明调用方式>

## 5. 实现方案

### 5.1 涉及文件

<用表格列出相关文件/类/模块>

### 5.2 类图

<mermaid classDiagram：新增/修改的类、结构体及其关系，包含关键属性和方法>

### 5.3 功能模块

适时的使用callout来追加信息。
按功能拆分为多个子章节，每个子章节结构如下：
在必要的地方标注注意事项，包括但不限于： 性能、兼容性、边界情况、踩坑点、风险项用 

#### 5.3.1 <模块1>

简要描述如下内容：
- 核心逻辑
- 功能目标
- 触发条件
- 模块角色

##### 5.3.1.1 伪代码

每个接口/函数单独一个代码块，代码块前用一句话说明职责和触发时机。

##### 5.3.1.2 图示

有选择性的给出图示，如果功能非常简单，则可以考虑跳过该部分。
优先选择流程图，如果设计的模块非常多，可以考虑使用时序图表达。

## 6. 调试接口
如果不需要调试接口，则跳过该章节

| CVar | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `Cfr.Debug.<模块>.<功能>` | bool | false | 开启调试可视化 |
| `Cfr.Debug.<模块>.Disable` | bool | false | 强制关闭该功能 |

## 7. 蓝图使用说明
如果不涉及蓝图，则跳过该章节
- 在哪个蓝图类中调用
- 调用时机
- 输入参数来源
- 输出结果如何使用

## 8. 待确认事项
如果没有，则跳过该章节

<还未澄清的问题，留作后续跟进>

## 9. 变更记录
一句话描述变更记录，避免详细内容

- **<YYYY-MM-DD HH:mm>**：初始创建 <br>