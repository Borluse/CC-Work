# 原始需求：storylite 插件优化

- 2026-08-02 00:06：初始创建
- 2026-08-02 00:07：按用户反馈，提问方式改为「一次性批量提出全部未决问题」，不再逐个提问
- 2026-08-02 00:09：设计确认；插件实现完成（SKILL.md 新增章节、Design 流程改写、版本 bump 0.1.1、README 同步）
- 2026-08-02 00:18：追加需求2（插件结构优化）；范围确认：Q1 选 1-4（章节归类/触发词路由表/模板补齐/状态枚举）、Q2 模板内联、Q3 元数据同步维持手工
- 2026-08-02 00:28：需求2 设计确认并实现完成（SKILL.md 章节重排、新增模式路由表与六类模板、约定新增状态枚举；版本 bump 0.1.2，README 同步）
- 2026-08-02 00:45：追加需求3（插件精简与 TDD 并入总览），追溯补录；B 档精简已实现（SKILL.md 254→192 行、9 模式→6、TDD.md 移除并入总览；版本 bump 0.1.3，README 同步）
- 2026-08-02 00:55：追加需求4（技能防再犯加固）；Q1 选 B（激活+定位硬门槛+双向铁律）、Q2 选 B（豁免精确化）

## 需求1：设计阶段提问规范

### 需求背景

story-lite 插件（v0.1.0）当前 SKILL.md 中与提问相关的规范只有一句：「每个问题附标题、原因、可选项、推荐项；禁止脑补，可查代码的用 ast-grep/rg/fd 查证」。缺少对「设计阶段如何提问」的完整约束，实际使用中容易一次性输出方案、替用户做决定、未确认细节就落文档。

### 需求描述

1. 在 `plugins/story-lite/skills/story-lite/SKILL.md` 中新增独立章节「设计阶段提问规范」，自包含地固化以下规则：
   - 与用户讨论方案/设计细节时，对每个细节持续深度追问，直到达成完全一致理解；
   - 提问采用批量方式：一次性提出当前设计树全部未决问题（Q 编号 + 四要素），不逐个提问；用户一次性答复，答复引出新问题时下一轮再批量提出，迭代直至完全一致；
   - 遍历设计树每个分支，逐一梳理解决各决策依赖关系；
   - 每个问题附推荐解答/建议；
   - 若问题可通过探索代码库得答案，主动用 ast-grep、rg、fd 检索相关逻辑后再做决策；
   - 提问时禁止使用 `askuserquestion` 工具；
   - 提问需包含：标题、原因、可选项、推荐选项及其原因；
   - 所有出现疑问/不确定的地方均按此方式提问，不预设用户答案、禁止脑补；
   - 细节全部确认前不落任何文档，全部确认后一次性落盘，再走文档确认循环。
2. 提问编号采用 Q 递增编号（Q1、Q2…），四要素必填，不强制分类与分隔符。
3. Design 模式流程改写：先按提问规范对话澄清，全部细节确认后再落盘设计文档。
4. 版本与文档同步：`.claude-plugin/plugin.json` → 0.1.1；`.codex-plugin/plugin.json` → 0.1.1+codex.20260802000627；根 README 版本号与 story-lite 描述同步更新。

### 验收标准

- SKILL.md 包含上述章节，Design 模式流程体现「先提问确认、后落盘」；
- 提问格式符合 Q 编号 + 四要素；
- 插件版本号与 README 已同步；
- 不修改 story-plugin 等其他插件内容。

## 需求2：插件结构优化

### 需求背景

story-lite 当前为单文件结构（SKILL.md 163 行 + 双 manifest），整体自包含且内部规则闭环，但结构上有 4 处可优化：`## Milestone 配置` 语义上嵌套在「设计阶段提问规范」章节内；工作流程缺少「触发词 → 模式」路由表；六类文档（原始需求、quick、TDD、设计文档、总览、review 报告）只有设计文档与 milestones.yaml 有显式模板；总览的「状态」字段无枚举、取值自由。以上均不涉及工作流步骤或效果变化。

### 需求描述

1. **章节归类**：SKILL.md 重排为「定位 → 文档结构与约定（含 Milestone 配置）→ 设计阶段提问规范 → 工作流程」，将 Milestone 配置从「设计阶段提问规范」下移出；在「约定」中新增总览状态枚举。
2. **触发词路由表**：工作流程开头新增「模式路由」小节，映射用户触发词到 9 个模式。
3. **模板补齐（全部内联）**：原始需求、quick、TDD、设计文档、总览、review 报告各补显式模板，内容与现有文字规则一致，不引入新规则。
4. **状态枚举**：约定中定义「待设计 / 设计中 / 已确认 / 实现中 / 已完成 / 已Review / 阻塞」，需求1 在总览中的状态表述归一为枚举值。

### 验收标准

- SKILL.md 章节顺序为「定位 → 文档结构与约定 → 提问规范 → 工作流程」，Milestone 配置不再嵌套在提问规范下；
- 工作流程开头有「触发词 → 模式」路由表；
- 六类模板全部内联且与现有规则一致；
- 总览状态使用枚举值；
- 工作流 9 个模式步骤、quick/正式规则、review 流程、提问规范内容保持不变；
- 不修改 story-plugin 等其他插件内容。

## 需求3：插件精简与 TDD 并入总览

### 需求背景

v0.1.2 的 SKILL.md（254 行）存在规则重复（变更记录、命名、代码为准各出现多处）与模式冗余（9 个模式中整合/总结本质相同、TDD 单独成模式、定位/需求整理过碎）；TDD.md 与总览功能重叠（需求点清单/依赖图）。经用户确认采用 B 档精简，并将 TDD 功能并入总览。

### 需求描述

1. 模式合并：9 个模式 → 6 个——「1 定位与需求整理」「2 Quick」「3 Design（含需求拆分）」「4 实现与同步维护」「5 Review」「6 整合与总结」；
2. TDD 并入总览：移除 TDD.md，需求点清单与依赖图写入 `总览.md`（多需求点在 Design 拆分时建立总览并落清单，单点在首个设计确认后建立）；总览模板新增「需求点清单」章节；
3. 规则去重：变更记录、命名、文档先行/代码为准、状态枚举等重复规则收敛到「约定」；删除失效的 `askuserquestion` 指令；
4. 模板保持内联，不拆 references；提问规范、确认循环、总览增量维护、review 落盘等约束保持不变；
5. 版本与文档同步：双 manifest → 0.1.3，README 同步。

### 验收标准

- SKILL.md 为 6 模式工作流，路由表同步收窄；
- 文档结构中无 TDD.md，总览模板含需求点清单；
- 重复规则各保留一处，无失效工具指令；
- 提问规范/文档确认循环/总览增量维护/review 流程行为不变；
- 版本号与 README 已同步。

## 需求4：技能防再犯加固

### 需求背景

需求3 实现过程中暴露流程漏洞：用户显式点名 story-lite 后，agent 以"与需求工作流无关的一次性讨论"为由跳过整个工作流（未定位、未维护任何 .agent 文档），直接裸改插件。根因：豁免条款过宽、点名后无强制第一步、铁律只管事后不管事前。

### 需求描述

1. 激活条款收紧（`# 定位`·使用前提）：点名即激活，第一动作执行「1 定位」并向用户展示定位结论，未完成前不得进入内容讨论或改动任何文件；
2. 豁免条款精确化：仅"无任何文件产出、无改动"的纯信息问答豁免；凡涉及新增/修改文件的任务一律走工作流，包括对本技能/插件本身的修改（元任务不豁免，续入相关 story）；
3. 模式 1 定位新增硬规则：点名后第一动作；定位结论先展示；已存在同主题 slug → 续用并追加需求点，不新建、不裸改；
4. 铁律升级为事前+事后双向（模式 4）：开工前无对应需求文档（原始需求 / quick 文档）不得改动文件；
5. 版本与文档同步：双 manifest → 0.1.4，README 同步。

### 验收标准

- SKILL.md 激活条款含"点名即激活、第一动作定位、元任务不豁免"表述；
- 豁免条款仅限无文件产出的纯信息问答；
- 模式 1 含"定位结论先展示、同主题续用 slug"规则；
- 模式 4 铁律含"无文档不开工"事前约束；
- 版本号与 README 已同步；.agent 文档（原始需求/设计/总览）同步更新。
