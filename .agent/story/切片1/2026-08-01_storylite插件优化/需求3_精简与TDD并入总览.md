# 需求3：插件精简与 TDD 并入总览

## 需求简介

对 story-lite 做 B 档结构精简：9 模式合并为 6，TDD 需求点清单/依赖图并入总览，规则去重；除 TDD 文件移除与总览生命周期调整外，工作流行为不变。

## 功能说明

- 策划目标：降低 SKILL.md 冗余（254 行 → 192 行）与工作流维护负担；TDD.md 与总览功能重叠，并入后每需求少维护一类文档。
- 技术行为：重写 `plugins/story-lite/skills/story-lite/SKILL.md`；bump 双 manifest 至 0.1.3；根 README 版本与描述同步。纯文档/配置改动，无运行时代码，不涉及网络同步与异常处理。

## 设计细节

### 决策记录（用户已确认）

| Q | 决策点 | 结论 |
|---|--------|------|
| Q1 | 精简档位 | B 档：结构合并（含 A 档去重）；不做 C 档功能裁减（原始需求合并、milestones 移除） |
| Q2 | TDD 去向 | 并入总览：移除 TDD.md，需求点清单与依赖图写入总览「需求点清单」章节（编号/名称/目标/功能范围/方案思路/依赖，依赖复杂时附 mermaid） |
| Q3 | 总览生命周期 | 多需求点：Design 拆分时建立总览并落清单；单点：首个设计确认后建立（仅一个章节） |
| Q4 | 模板落位 | 全部内联保留，不拆 references（小技能外移模板得不偿失） |
| Q5 | 去重范围 | 变更记录、命名、文档先行/代码为准、状态枚举各收敛一处；删除失效的 `askuserquestion` 指令（需求1 决策的相应表述同步废止） |
| Q6 | 版本与 README | 0.1.2 → 0.1.3，双 manifest + README 手工同步（沿用需求2 Q4 结论） |

### 变更要点

1. 模式路由 9 → 6：
   - 1 定位 + 2 需求整理 → 1 定位与需求整理
   - 4 TDD + 5 Design → 3 Design（含需求拆分）
   - 8 整合 + 9 总结 → 6 整合与总结
2. 文档结构：移除 TDD.md；总览承担「需求点清单/依赖 + 增量维护」
3. 总览模板新增「需求点清单（多需求点）」表 + 可选 mermaid 依赖图
4. 提问规范：章节名「设计阶段提问规范」→「提问规范（所有环节适用）」，移除失效工具指令
5. 规则收敛：命名/变更记录/状态枚举/文档先行集中到「约定」，各出现一次

## 变更记录

- 2026-08-02 00:45：追溯补录——本需求于 00:29 起直接实现（SKILL.md 重写、254→192 行），未先落设计文档；按工作流铁律补录原始需求/设计/总览，版本 bump 0.1.3 与 README 同步一并完成
